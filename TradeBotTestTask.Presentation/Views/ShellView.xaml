<UserControl x:Class="TradeBotTestTask.Presentation.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TradeBotTestTask.Presentation.Views"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5">
            <TextBlock Text="Pair:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Width="100" Text="{Binding Pair, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Connect"   Margin="6,0" cal:Message.Attach="Connect"   IsEnabled="{Binding CanConnect}"/>
            <Button Content="Disconnect" cal:Message.Attach="Disconnect" IsEnabled="{Binding CanDisconnect}"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Trades}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"   Binding="{Binding Time}" Width="*"/>
                <DataGridTextColumn Header="Side"   Binding="{Binding Side}" Width="60"/>
                <DataGridTextColumn Header="Price"  Binding="{Binding Price}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
